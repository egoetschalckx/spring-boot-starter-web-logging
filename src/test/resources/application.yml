logging:
  json:
    pretty-print: true
  web:
    client:
      enabled: true
      include-body: true
    server:
      enabled: true
      include-body: true

spring:
  profiles:
    active: log-json
